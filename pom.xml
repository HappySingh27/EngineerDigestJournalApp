<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.3</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.dehlan</groupId>
	<artifactId>Journal</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>myFirstJournal</name>
	<description>Demo project for Spring Boot</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>17</java.version>
		<sonar.host.url>http://localhost:9000</sonar.host.url>
		<sonar.login>squ_8800e0fffea5ab9935a9ceb11aad53724d0123eb</sonar.login>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- https://mvnrepository.com/artifact/org.springframework.boot/spring-boot-starter-data-mongodb -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-mongodb</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<version>1.18.30</version> <!-- Use the latest stable version -->
			<!--scope>provided -->
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>

		<!--
  JJWT API
  🔹 Definition: Core interfaces and classes for working with JWTs (e.g., JwtBuilder, JwtParser).
  🔹 Purpose: Provides the contracts you use to create, sign, and parse JWTs in your code.
  🔹 Use: Add this dependency to write code against the JJWT API without depending on implementation details.
-->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.11.5</version>
		</dependency>

		<!--
          JJWT Implementation
          🔹 Definition: The actual implementation logic of the JJWT API (signing, verification, key handling).
          🔹 Purpose: Provides runtime functionality behind the API interfaces.
          🔹 Use: Required at runtime to actually generate and validate JWTs. Kept in runtime scope so your code doesn’t compile against internal classes.
        -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.11.5</version>
			<scope>runtime</scope>
		</dependency>

		<!--
          JJWT JSON Serializer (Jackson)
          🔹 Definition: Integration module that allows JJWT to use Jackson for converting JWT claims/payloads into JSON and back.
          🔹 Purpose: JWT payloads are JSON, so a serializer is needed. This module plugs Jackson into JJWT.
          🔹 Use: Required at runtime to handle serialization/deserialization of JWT bodies.
                 Alternatives exist like jjwt-gson or jjwt-orgjson if you prefer another JSON library.
        -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.11.5</version>
			<scope>runtime</scope>
		</dependency>


	</dependencies>

	<build>
		<plugins>
			<plugin>
				<!--
  🔧 What is a Maven Plugin?
  - Plugins extend Maven's build capabilities.
  - `maven-compiler-plugin` specifically handles Java code compilation.
  - Needed when using annotation processors like Lombok.

  ❓ Why app was running in IDE but `mvn compile` failed?
  - already had lombok plugin, I had installed lombok plugin
  - But Maven CLI needs explicit config to recognize Lombok-generated methods.

  ❌ Problem:
  - Without this plugin + annotationProcessorPaths, Maven fails with:
    "cannot find symbol" for getters/setters/builders generated by Lombok.

  ✅ Solution:
  - Added `maven-compiler-plugin` with version 3.14.0.
  - Defined `<annotationProcessorPaths>` for Lombok.
  - Maven now properly compiles all Lombok-based code.

  💡 Summary:
  - Lombok requires annotation processing.
  - IDE may hide this issue, but Maven build fails without proper plugin config.
-->

				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.14.0</version>
				<configuration>
					<source>17</source>
					<target>17</target>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
							<version>1.18.30</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.sonarsource.scanner.maven</groupId>
				<artifactId>sonar-maven-plugin</artifactId>
				<version>3.11.0.3922</version> <!-- Use latest available -->
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>

	</build>

</project>
